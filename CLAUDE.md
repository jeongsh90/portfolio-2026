## 하네스: 포트폴리오 랜딩페이지 (D:\MyCloud\2026포트폴리오)

**목표:** HTML + CSS + JavaScript + GSAP로 만드는 정적 포트폴리오 랜딩페이지를 기획(섹션 구성/콘텐츠 필드)부터 디자인(토큰/레이아웃/모션 컨셉), 구현(반응형+ScrollTrigger 애니메이션), QA(반응형·애니메이션·성능·접근성 실검증), git/GitHub 연결까지 에이전트 팀으로 구축한다. 빌드 도구·프레임워크 없는 순수 정적 사이트가 원칙.

**트리거:** 포트폴리오/랜딩페이지/섹션 추가·수정/디자인/GSAP 애니메이션/반응형/git 연결/GitHub 업로드 관련 작업 요청 시 `portfolio-landing-orchestrator` 스킬을 사용하라. 후속 수정·재실행·특정 섹션만 다시·콘텐츠를 실제 내용으로 교체·QA 재검증 요청도 동일 스킬로 처리한다. 단순 질문은 직접 응답 가능.

**배포:** GitHub Pages로 공개 접속 가능 — https://jeongsh90.github.io/portfolio-2026/ (저장소 `main` 브랜치 루트를 소스로 사용, `push` 후 보통 1~2분 내 자동 반영).

**Git 커밋 정책 (반드시 준수):** 이 저장소는 공개(public) GitHub 저장소다.
- `CLAUDE.md`(이 파일), `.claude/`(하네스 에이전트·스킬 정의), `_workspace/`의 현재 산출물(00_input/01_planner_ia/01b_addendum/02_designer_system/03_developer_notes)은 **커밋한다** — 다른 컴퓨터에서 clone해 이어서 작업할 때 하네스와 지금까지의 작업 맥락을 그대로 가져오기 위함(2026-08-24, "하네스 구성한거 커밋해 다른데서도 이어서 작업하게" 요청으로 기존 "커밋 금지" 정책을 뒤집음 — 공개 저장소라 내부 기록이 그대로 노출된다는 점은 사용자가 인지하고 선택함).
- 다음은 여전히 **커밋하지 않는다**(`.gitignore`에 등록돼 있으니 유지할 것): `_workspace/_prev_multiview/`·`_workspace/_prev_gsap_rebuild/`(폐기된 과거 구현 전체 사본 — 용량만 크고 이어서 작업하는 데 불필요), `HorizontalSmoothScrollLayout-main/`(사용자가 참고용으로 제공한 외부 레퍼런스 자산 — 우리 콘텐츠 아님, 실제로 커밋된 적 없음을 `git log --all`로 확인함).

새 파일을 이 경로들 안에 추가하거나 이 정책 자체를 바꿀 때도 `.gitignore`가 위 두 가지(폐기 구현 백업, 외부 레퍼런스)만 계속 가리키는지 커밋 전에 확인한다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-23 | 초기 구성 (4개 에이전트: 기획/디자인/개발(GSAP+git)/QA, 5개 스킬 + 오케스트레이터, 에이전트 팀 모드) | 전체 | "포트폴리오 랜딩페이지를 HTML/CSS/JS/GSAP로 만들고 git 연결" 요청. Git 범위는 "로컬 init + GitHub 원격 연결", 콘텐츠는 "아직 없음 — 플레이스홀더 구조부터"로 확인 |
| 2026-08-23 | **범위 대폭 축소** — 인트로/포트폴리오/연혁/자기소개 4-뷰 해시라우팅 SPA 설계를 폐기하고, **단일 포트폴리오 페이지 하나만** 만드는 것으로 재구성. 사용자가 제공한 참고 자산 `HorizontalSmoothScrollLayout-main/`(Codrops 데모, Locomotive Scroll 기반 가로 스크롤 갤러리)의 **데모2를 GSAP(스무스스크롤+ScrollTrigger)로 동일하게 재구현**하는 것이 현재 유일한 목표. 연혁·자기소개는 이번 범위에서 뺌(추후 필요시 재추가 가능하도록 기존 4-뷰 산출물은 `_workspace/_prev_multiview/`에 보존). "일단 페이지 구성하고 나면 이후 명령"이라 밝혀, 이후 세션에서 요구사항이 다시 확장될 수 있음 — `portfolio-planner`/`portfolio-designer` 에이전트와 그 스킬은 삭제하지 않고 향후 확장(페이지 추가 등) 시 재사용 대기 | CLAUDE.md, 루트 index.html/css/js(전면 재작성 예정), `.claude/skills/portfolio-landing-orchestrator` | "하네스를 다시 구성해 — 데모2를 활용해서 단일 포트폴리오 페이지를 만들꺼야, 똑같이 만드는데 gsap로 다시만들면돼, 연혁·자기소개는 뺄꺼야, gsap 스무스스크롤·스크롤트리거" 요구사항 |
| 2026-08-23 | 바로 위에서 GSAP로 새로 재구현한 버전(마크업/CSS/JS 전부 새로 작성, 스킨은 데모2와 유사하게)을 사용자가 곧바로 다시 리셋 — **재구현하지 말고 데모2 원본 파일을 그대로 가져오는 것**으로 변경. GSAP 재구현물은 `_workspace/_prev_gsap_rebuild/`에 보존. `HorizontalSmoothScrollLayout-main/HorizontalSmoothScrollLayout-main/dist/`(Parcel로 이미 빌드된 정적 산출물 — 소스의 ES 모듈/Locomotive Scroll import를 브라우저에서 바로 실행 가능한 번들로 변환해둔 것)에서 `index2.html`→`index.html`, `base.98fd6c19.css`/`demo2.06b37b5f.css`/`demo2.44794d1a.js`/`favicon.26242483.ico`와 참조된 이미지 12장을 파일명·경로 그대로 프로젝트 루트에 복사. 즉 지금은 **Locomotive Scroll 기반 데모2 원본 그대로**이며 GSAP가 아님 — Playwright로 실제 렌더링(이미지 회전 트릭, Typekit moret 서체, explore 버튼, 태그) 정상 확인. 콘텐츠도 데모 원본 그대로(스톡사진 인물 12장, 영문 카피)라 아직 포트폴리오 실제 콘텐츠로 치환되지 않은 상태 | 루트 index.html/base.98fd6c19.css/demo2.06b37b5f.css/demo2.44794d1a.js/favicon.26242483.ico/1~12번 jpg(신규, 데모2 dist 원본 그대로) | "리셋하고 그냥 데모2가져와" 요구사항 |
| 2026-08-23 | "드래그로도 슬라이드가 넘어갈 수 있게끔" 요청 반영 — 원본 데모2는 Locomotive Scroll의 `useTouch` 옵션 덕분에 터치 기기에서만 드래그가 되고, 마우스로는 휠(또는 트랙패드 세로 제스처)만 먹혔다. 처음엔 마우스 드래그 이동량을 합성 `WheelEvent`로 만들어 `window.dispatchEvent`하는 방식으로 시도했으나 실제로 갤러리가 전혀 안 움직여 원인을 실측 — Locomotive의 `smooth:true` 모드는 `VirtualScroll`의 리스너를 `window`가 아니라 `document`에 건다는 걸 번들 소스에서 직접 확인했고, 그보다 더 근본적으로는 라이브러리가 이벤트를 어디서 받는지와 무관하게 신뢰할 수 있는 경로가 필요하다고 판단해 접근을 바꿨다 — 데모2 번들(`demo2.44794d1a.js`)에서 `LocomotiveScroll` 인스턴스를 생성하는 지점 바로 아래에 `window.__lscroll = lscroll;` 한 줄만 추가해 인스턴스를 외부에서 접근 가능하게 하고, 새 `drag-scroll.js`가 실제 마우스 드래그 이동량을 라이브러리가 휠/터치 입력에 실제로 쓰는 바로 그 내부 메서드(`lscroll.scroll.updateDelta({deltaY})` + `startScrolling()`)에 직접 흘려보내도록 구현 — 합성 이벤트를 흉내 내지 않고 라이브러리의 실제 내부 갱신 경로를 그대로 타므로 관성/부드러움이 휠 스크롤과 완전히 동일하다. 터치는 `pointerType==='touch'`일 때 핸들러를 건너뛰어 기존 Locomotive 터치 드래그와 충돌하지 않게 함. 드래그 중 커서를 `grab`/`grabbing`으로 바꾸고 텍스트 선택을 막는 CSS도 추가. Playwright로 실제 `page.mouse` 드래그를 왼쪽/오른쪽 양방향으로 재현해 갤러리가 정확한 방향으로 이동함을 확인(왼쪽 드래그=다음 항목으로 전진, 오른쪽 드래그=이전 항목으로 복귀) | demo2.44794d1a.js, drag-scroll.js(신규), demo2.06b37b5f.css, index.html | "드래그로도 슬라이드가 넘어갈 수 있게끔" 요구사항 |
| 2026-08-23 | 사용자가 로컬 `C:\Users\user\Downloads\RixYeoljeongdo+Regular.ttf`(무료 한글 디스플레이 폰트) 등록 요청 — `fonts/RixYeoljeongdo-Regular.ttf`로 복사해 `@font-face`(`font-family: 'Rix Yeoljeongdo'`)로 등록만 해두고, 기존 갤러리 번호/타이틀(현재는 데모 원본의 영문 플레이스홀더 텍스트가 그대로 있고 Typekit `moret` 이탤릭 세리프를 쓰는 중)에는 아직 적용하지 않았다 — 실제 한글 콘텐츠(프로젝트명 등)로 교체되는 시점에 이 폰트를 어디에 쓸지 정하는 게 더 자연스럽다고 판단했기 때문. Playwright로 `document.fonts`에 정상 로드되는 것까지만 확인 | fonts/RixYeoljeongdo-Regular.ttf(신규), demo2.06b37b5f.css | "RixYeoljeongdo+Regular.ttf 폰트 등록" 요구사항 |
| 2026-08-24 | "하네스 구성한거 커밋해 다른데서도 이어서 작업하게" — 어제 세운 "CLAUDE.md·.claude/·_workspace/는 공개 저장소에 커밋 금지" 정책을 사용자가 명시적으로 뒤집음(다른 컴퓨터에서 clone해 하네스·작업 맥락을 그대로 이어가야 하니 공개 노출을 감수하고서라도 커밋하길 원함). `.gitignore`에서 `.claude/`/`CLAUDE.md`/`_workspace/` 제외 규칙을 제거하되, 폐기된 과거 구현 전체 사본(`_workspace/_prev_multiview/`, `_workspace/_prev_gsap_rebuild/`, 합쳐서 약 580KB — 이어서 작업하는 데는 불필요)과 외부 레퍼런스 자산(`HorizontalSmoothScrollLayout-main/`)만 계속 제외. 이 파일 상단의 "Git 커밋 정책" 절도 새 방침으로 갱신 | .gitignore, CLAUDE.md("Git 커밋 정책" 갱신), .claude/**, _workspace/{00_input,01_planner_ia.md,01b_addendum_main_page.md,02_designer_system.md,03_developer_notes.md}(신규 커밋) | "하네스 구성한거 커밋해 다른데서도 이어서 작업하게" 요구사항 |
| 2026-08-24 | "깃에 외부접속 링크 만들어" — GitHub Pages를 `gh api`로 활성화(`POST /repos/{owner}/{repo}/pages`, source=`main` 브랜치 루트). 별도 빌드 과정 없는 정적 사이트라 추가 설정 없이 바로 배포됨. 배포 URL: https://jeongsh90.github.io/portfolio-2026/ (파일 최상단 "배포" 절에 기록). 배포 직후엔 몇 분간 404가 뜰 수 있어 반복 폴링으로 실제 200 응답을 받을 때까지 확인한 뒤, Playwright로 실제 브라우저에서 이미지·폰트·스크립트가 전부 정상 로드되는 것까지 재확인 | (GitHub 저장소 설정 — 로컬 파일 변경 없음), CLAUDE.md("배포" 절 추가) | "깃에 외부접속 링크 만들어" 요구사항 |
| 2026-08-24 | "커밋하는데 md내용, 로컬정보, HorizontalSmoothScrollLayout-main폴더는 커밋하지마 기록해" — 공개 저장소에 내부 작업 기록·하네스 설정이 노출되지 않도록 `CLAUDE.md`, `.claude/`, `_workspace/`를 git 추적에서 제외(`git rm --cached`로 인덱스에서만 제거 — 로컬 파일은 그대로 유지)하고 `.gitignore`에 등록. `HorizontalSmoothScrollLayout-main/`은 이미 처음부터(첫 커밋 전부터) `.gitignore`에 있었고 `git log --all --full-history`로 실제 커밋 이력이 전혀 없음을 확인 — 별도 정리 불필요. 이 정책 자체를 이 파일 상단(트리거 바로 아래)에 "Git 커밋 정책"으로 명시해 다음 세션에서도 지켜지도록 기록 | .gitignore, (CLAUDE.md·.claude/·_workspace/ 추적 해제 — 로컬엔 계속 존재) | "md내용, 로컬정보, HorizontalSmoothScrollLayout-main폴더는 커밋하지마 기록해" 요구사항 |
| 2026-08-24 | "css, js 파일명도 변경, 데모 이런거 쓰지말고" — 데모2 원본을 그대로 복사해올 때 Parcel 빌드가 붙인 무의미한 해시와 "demo2" 문구가 그대로 남아있던 파일명 3개를 의미가 드러나는 이름으로 정리: `base.98fd6c19.css`→`base.css`(리셋+Locomotive Scroll 기본 스타일), `demo2.06b37b5f.css`→`gallery.css`(갤러리 아이템·태그·링크·섹션 스타일), `demo2.44794d1a.js`→`gallery.js`(Locomotive Scroll+커서+스큐 효과 번들, gallery.css와 짝이 되는 이름). `drag-scroll.js`/`modal.css`/`modal.js`는 이미 의미가 분명해 그대로 둠. CSS 파일명 자체를 바꿔도 그 안의 `@font-face url('../fonts/...')`처럼 파일 "위치" 기준 상대경로는 영향받지 않음(파일명이 아니라 디렉토리가 기준이라)을 확인하고 index.html의 `<link>`/`<script src>` 경로만 갱신. Playwright로 새 경로 전부 200 응답, 모달 오픈까지 재확인. 페이지 `<title>`/메타 설명에 남아있는 "Demo 2 | Codrops" 문구는 이번 요청(파일명)의 범위 밖이라 손대지 않음 | index.html, css/{base,gallery}.css(이름변경), js/gallery.js(이름변경) | "css, js 파일명도 변경 데모 이런거 쓰지말고" 요구사항 |
| 2026-08-24 | "js,css 파일 구분해" 요청으로 루트에 평평하게 흩어져 있던 파일들을 정리 — `css/`(base.98fd6c19.css, demo2.06b37b5f.css, modal.css)와 `js/`(demo2.44794d1a.js, drag-scroll.js, modal.js) 폴더를 새로 만들어 각각 이동하고, index.html의 `<link>`/`<script src>` 경로를 전부 갱신. CSS의 `url()`은 HTML이 아니라 그 스타일시트 파일 자신의 위치를 기준으로 상대경로가 풀리기 때문에, `demo2.06b37b5f.css`의 `@font-face src: url('fonts/...')`가 파일 이동 후에도 그대로였다면 `css/fonts/...`를 찾아 깨졌을 것 — `../fonts/...`로 함께 고쳐 실제로 안 깨지게 함(이미지·파비콘·폰트 폴더는 이번 요청 범위 밖이라 루트에 그대로 둠). Playwright로 모든 새 경로가 200으로 응답하는 것, 폰트가 실제로 로드되는 것, 모달 오픈까지 재확인 | index.html, css/*(이동), js/*(이동) | "js,css 파일 구분해" 요구사항 |
| 2026-08-24 | 모달 닫기(X) 버튼에 원형 보더 라인이 보이던 문제 수정 — `border:none`은 이미 있었지만 `outline`은 한 번도 지정한 적이 없어, 모달이 열릴 때마다 접근성을 위해 JS에서 호출하던 `closeBtn.focus()`가 브라우저 기본 포커스 링(회색 원)을 그대로 노출시키고 있었다. 무조건 `outline:none`으로 지우는 대신 `:focus-visible`에만 커스텀 링(`--color-gallery-title` 2px)을 지정 — 이 의사클래스는 마우스 클릭이나 스크립트의 `.focus()` 호출로는 거의 트리거되지 않고 실제 Tab 키 탐색일 때만 나타나므로, 마우스로 이미지를 클릭해 모달을 열 때(보편적 사용 흐름)는 링이 안 보이고 키보드로 접근성 있게 순회할 때는 계속 보인다. 이 작업 도중 사용자가 `modal.css`의 `.modal__info`를 직접 편집(`max-width:32rem` 주석 처리 + `width:100%` 추가)한 것을 확인 — 그대로 두고 손대지 않음 | modal.css | "보더라인 안생기게" 요구사항(닫기 버튼을 원으로 표시한 스크린샷) |
| 2026-08-24 | 사용자가 개발자도구 요소 패널 스크린샷에서 `.modal__body`를 직접 원으로 짚으며 "썸네일섹션의 텍스트영역이니까 맞게끔 클래스명 변경, 다른데도 명칭이 잘못된곳 수정" 요청 — 클래스명이 실제 구조/역할과 어긋난 곳을 전수 점검해 4곳을 정리: (1) `.modal__body`→`.modal__info`("모달 전체의 body"처럼 읽혀 오해를 주던 것을, 실제로는 첫 섹션(썸네일) 옆의 정보 텍스트 영역이라는 뜻이 드러나게) (2) `.modal__stage`→`.modal__hero`(이미지+정보로 구성된 "첫 섹션"이라는 성격이 이름에 안 드러났던 것을, 이후 이어지는 `.modal__section`들과 구분되는 "히어로" 섹션임을 명확히) (3~4) 지난번 "메인과 동일한 세로 스무스스크롤" 구현 때 급하게 붙인 `.modal__scroll`(고정 뷰포트, overflow:hidden)/`.modal__scroll-inner`(실제로 transform으로 움직이는 콘텐츠) 조합이 이름과 실제 역할이 뒤바뀌어 있던 것(정작 "스크롤"되는 건 -inner 쪽인데 이름은 바깥쪽에 붙어있었음)도 함께 바로잡음 — 바깥 고정 뷰포트를 `.modal__viewport`로, 실제로 움직이는 콘텐츠 레이어를 `.modal__scroll`로 교체. 미사용 상태로 남아있던 `id="modalScroll"`도 `id="modalViewport"`로 함께 정리. JS 변수명도 클래스명과 다시 짝이 맞도록 `scrollWrap`→`viewport`, `scrollInner`→`scrollEl`, `body`→`info`로 함께 변경(DOM 참조·이벤트 바인딩 전부 갱신, 로직은 그대로). Playwright로 모달 열기/텍스트 fade-in/세로 스무스스크롤/닫기까지 이름을 바꾼 뒤에도 전부 정상 동작하는 것을 재확인 | index.html, modal.css, modal.js | "썸네일섹션의 텍스트영역이니까 맞게끔 클래스명 변경, 다른데도 명칭이 잘못된곳 수정" 요구사항 |
| 2026-08-24 | "모달 첫번째 섹션 높이는 100vh" — `.modal__stage`(이미지+텍스트, 섹션 1)가 그동안 `min-height:100%`라 텍스트 콘텐츠가 길면 한 화면보다 더 늘어나 다른 100vh 섹션들과 리듬이 안 맞았던 것을, 나머지 섹션(`.modal__section`)과 동일하게 `height:100vh` 고정으로 변경. 고정 높이로 바꾸면서 텍스트(현재 9개 문단 placeholder)가 100vh보다 길어질 경우 그대로 두면 섹션 2 영역까지 시각적으로 겹쳐 보이는 문제가 생겨, `overflow:hidden`을 함께 추가해 그 섹션 안에서 깔끔하게 잘리도록 함(넘치는 문단은 지금 잘 안 보이지만, 실제 콘텐츠로 교체될 placeholder 단계라 우선 리듬 통일을 택함). Playwright로 실제 뷰포트 기준 `.modal__stage`의 `getBoundingClientRect().height`가 정확히 900px(뷰포트 100vh)로 고정된 것과, 하단 텍스트가 섹션 경계에서 겹침 없이 잘리는 것을 확인 | modal.css | "모달 첫번째 섹션높이은 100 vh" 요구사항 |
| 2026-08-24 | 위 관성 수정 이후에도 "스크롤이 모달이 아니라 뒤쪽에 생기고 충돌난다"는 재확인(브라우저 기본 세로 스크롤바가 메인 화면 오른쪽에 뜨는 스크린샷 포함) — 실측으로 진짜 원인을 다시 좁힘: 관성 잔여분 문제와는 별개로, `.modal`을 스크롤 컨테이너(`overflow-y:auto`)로 만든 상태에서 내부 콘텐츠 스크롤이 상/하단 경계에 닿으면 브라우저 기본 동작인 "스크롤 체이닝"이 발생해 남은 휠 델타가 다음 스크롤 가능 조상(문서)으로 넘어가려는 것이 실제 원인이었다. 사용자가 "modal__backdrop 안쪽에 태그를 하나 더 감싸서 핸들링하게끔"이라 제안한 방향대로, 배경(`.modal__backdrop`, 순수 시각+클릭투클로즈 전용)과 실제 스크롤을 담당하는 요소를 분리 — 새 `.modal__scroll`(`position:fixed;inset:0`) 래퍼를 추가하고 거기에 `overscroll-behavior:contain`을 지정해 경계에서 체이닝 자체를 원천 차단(표준 스펙 프로퍼티, 정확히 이 종류의 버그를 위해 만들어짐). 이어서 사용자가 "메인에 적용된 스무스스크롤 그대로 세로 방향으로 적용해, 기본 스크롤 말고"라고 요청 — 메인 갤러리가 쓰는 Locomotive Scroll 인스턴스를 그대로 재사용하면 라이브러리가 `document.documentElement`에 `data-scroll-direction` 속성과 `has-scroll-*` 클래스를 전역으로 쓰기 때문에 세로 방향 두 번째 인스턴스를 같이 띄우면 가로 갤러리의 방향 속성을 덮어써 버릴 위험이 있어(실행 대신 코드 검토로 사전에 피함) 라이브러리를 재사용하지 않고, 이미 로드된 GSAP만으로 같은 느낌(lerp 관성 감속)을 직접 구현 — `.modal__scroll`을 순수 뷰포트(`overflow:hidden`)로, 그 안의 `.modal__scroll-inner`를 `transform:translateY()`로 움직이는 가상 스크롤 계층으로 분리하고, 휠/터치/키보드(방향키·Page Up·Down·Home·End) 입력을 받아 목표값을 누적한 뒤 매 프레임 `current += (target-current)*0.1`로 부드럽게 수렴시키는 컨트롤러를 modal.js에 신설. Playwright로 실측 확인한 것: (1) 단일 휠 이벤트 이후 여러 프레임에 걸쳐 transform 값이 점진적으로 목표치에 수렴하는 것(순간 이동이 아님) (2) 모달 콘텐츠 경계를 넘어서까지 계속 휠을 줘도 `document.documentElement.scrollHeight`가 뷰포트 높이와 정확히 일치하게 유지되는 것(더 이상 배경으로 새지 않음) (3) 화면상에 브라우저 기본 스크롤바가 전혀 뜨지 않는 것 (4) 이미지 FLIP-in 애니메이션·배경 스크롤 잠금·닫기 후 배경 스크롤 정상화까지 전부 기존과 동일하게 동작하는 것 | index.html, modal.css, modal.js | "스크롤이 아니라 뒤쪽에 생성되고있어 충돌나는거 같은데", "modal__backdrop 안쪽에 태그를 하나더 감싸서 핸들링하게끔", "메인에 적용된 스무스스크롤 똑같이 적용해 단, 세로방향" 요구사항 |
| 2026-08-23 | "모달창에서 스크롤이 아니라 뒤쪽(배경 갤러리)에서 생성되고 있어 충돌나는거 같다" 제보 — 실측으로 재현: 모달을 열기 직전에 배경 갤러리를 빠르게 휠 스크롤한 뒤 곧바로 이미지를 클릭하면, 모달이 열리고(`lscroll.stop()` 호출) 1.5~2초가 지나도 배경 갤러리 위치(`main`의 transform)가 계속 밀리는 것을 확인. 원인은 Locomotive Scroll의 내부 구조에 있었다 — 휠 이벤트가 들어올 때마다 `stop` 플래그를 그 자리에서 한 번 확인하고 통과하면 `requestAnimationFrame`으로 실제 위치 갱신(`updateDelta`)을 예약하는데, 빠르게 연속으로 휠 이벤트가 여러 번 들어오면 그만큼 rAF 콜백이 여러 개 대기열에 쌓인다 — 이 콜백들은 예약된 시점의 `stop` 통과 여부만으로 실행되므로, 그 직후 모달을 열며 `stop()`을 호출해도 **이미 예약된 콜백들은 취소되지 않고 이후 몇 프레임에 걸쳐 그대로 실행**되며 배경을 계속 밀어낸다(이전 세션에 "빠르게 스크롤하던 경우에 한해 짧은 잔여 이동이 있을 수 있는 사소한 한계"로 남겨뒀던 것이 실제로는 이렇게 뚜렷한 버그였음). 해결: `lockScroll()`에서 `stop()` 직후 한 번만 현재 위치로 `setScroll()`을 호출하던 것을, 모달을 여는 시점부터 약 20프레임(≈0.3초) 동안 매 프레임 `setScroll(현재 위치, 현재 위치)`를 반복 호출하는 "홀드" 루프로 교체 — 대기 중이던 rAF 콜백이 그 사이 실행돼 위치를 다시 밀어내더라도, 바로 다음 프레임에서 즉시 원위치로 되돌려 눈에 보이는 드리프트를 흡수한다. Playwright로 동일한 재현 시나리오(빠른 휠 스크롤 직후 즉시 클릭)를 다시 돌려 모달이 열린 뒤 배경 위치가 완전히 고정되는 것, 모달 자체 스크롤은 정상 동작하는 것, 닫은 뒤에는 배경 스크롤이 다시 정상 작동하는 것까지 확인 | modal.js | "스크롤 오류가있어 모달 창에서 스크롤이 아니라 뒤쪽에 생성되고있어 충돌나는거 같은데" 요구사항 |
| 2026-08-23 | "스크롤이 안 보여" — 뷰포트가 넉넉한 데스크톱 해상도에서는 모달 콘텐츠(이미지+텍스트 한 섹션)가 한 화면에 거의 다 들어와 방금 구현한 "모달 전체 스크롤"이 체감되지 않는다는 제보. 사용자가 이미 예고한 "아래로 섹션들을 추가할 예정"을 앞당겨, `100vh` 빈 섹션(`.modal__section`) 3개를 첫 섹션(이미지+텍스트) 바로 뒤에 추가해 실제로 명확히 스크롤이 체감되도록 함 — 각 섹션은 "섹션 2/3/4" 라벨(추후 실제 콘텐츠로 교체 예정, 지금은 빈 자리표시자)과 옅은 상단 구분선만 가진 최소 뼈대. 라벨 색상을 처음엔 갤러리 번호와 같은 저대비 "고스트" 톤(`--color-gallery-number`)으로 넣었다가, 스크롤 확인이 목적인 테스트용 마커라 오히려 눈에 잘 띄어야 한다고 판단해 고대비 크림톤(`--color-gallery-title`)으로 교체. Playwright로 뷰포트를 표준 데스크톱(1440×900)으로 되돌린 상태에서도 모달 전체 스크롤 높이가 3600px(섹션 4개×900px)로 뚜렷하게 늘어난 것과, 실제 스크롤 시 섹션 2·3이 순서대로 올라오는 것을 확인 | index.html, modal.css | "스크롤이 안보여 빈 섹션들 만들어봐 100vh로 3개" 요구사항 |
| 2026-08-23 | 모달 스크롤 구조를 "우측 텍스트만 내부 스크롤"에서 "모달 전체가 스크롤"로 전면 변경, 닫기 애니메이션도 단순화 — 사용자가 "이미지가 닫을 때 원래 자리로 안 돌아가도 되고, 모달창 전체 영역이 스크롤되게끔"이라 요청하고, 곧이어 스크린샷과 함께 "이미지+텍스트 영역이 하나의 섹션이고 아래로 섹션들을 추가할 예정"(쿠팡 상품 상세페이지의 좌측 썸네일/우측 정보 영역을 참고 예시로 제시)이라 설명해 의도를 명확히 함 — 지금의 이미지+텍스트 블록은 앞으로 이어질 여러 섹션 중 첫 섹션일 뿐이라는 것. 이에 맞춰 `.modal`(뷰포트 고정 컨테이너) 자체를 스크롤 컨테이너로 바꾸고(`overflow-y:auto`), `.modal__stage`는 `height:100%`(고정)에서 `min-height:100%`로 바꿔 콘텐츠가 한 화면보다 길면 자연스럽게 늘어나며 스크롤을 유발하게 함(고정 height를 유지한 채였다면 `align-items:center`가 콘텐츠를 컨테이너보다 위로도 밀어올려 스크롤로도 닿지 않는 영역이 생겼을 것 — 실측 대신 CSS 박스 모델을 재검토해 사전에 피함). 배경(`.modal__backdrop`)은 `position:absolute`였던 걸 `position:fixed`로 바꿔야 했는데, absolute인 채로 스크롤 컨테이너가 되면 배경 자신도 콘텐츠와 함께 스크롤되어 아래로 내리면 어두운 배경이 사라지는 문제가 있었을 것이기 때문. 우측 텍스트(`.modal__body`)에 있던 자체 `max-height`+`overflow-y:auto`(내부 스크롤)는 제거 — 이제 스크롤은 모달 전체가 담당. 텍스트 패널 자체 스크롤이 없어졌으므로 `.modal__body`의 좌측 이미지가 사라져도 되는 만큼, 닫기 애니메이션에서 이미지를 원래 썸네일 자리로 되돌리던 역-FLIP 로직 전체를 걷어내고 이미지·텍스트·닫기버튼·배경이 함께 페이드(+이미지만 살짝 축소)되는 단순한 페이드아웃으로 교체(열 때의 FLIP-in은 그대로 유지 — 이번 요청은 닫힘에만 해당). Playwright로 뷰포트를 일부러 낮춰(600px) 콘텐츠가 실제로 넘치는 상태를 만든 뒤 `.modal`을 스크롤해 이미지+텍스트가 함께 위로 밀려 올라가고 배경(블러+어둡게)은 뷰포트에 고정된 채 유지되는 것, 닫기가 별문제 없이 즉시 사라지는 것까지 확인 | modal.css, modal.js | "모달창에서 이미지가... 원래 있던자리로 안돌아가도 되고 모달창 전체 영역이 스크롤되게끔", "이미지 + 텍스트영역이 하나의 섹션이고 아래로 섹션들을 추가할 예정", "쿠팡을 예로들면 왼쪽 썸네일영역 우측 정보영역처럼" 요구사항 |
| 2026-08-23 | 모달 우측 텍스트 패널이 세로로 스크롤되게끔 — `.modal__body`에 `max-height:80vh(모바일 40vh); overflow-y:auto`를 추가해, 내용이 짧으면 기존처럼 그대로 보이고 길면 그 박스 안에서만 스크롤되도록(왼쪽 이미지는 계속 고정) 함. 실제로 스크롤이 필요한 상태를 확인하려면 콘텐츠가 있어야 해서 "일단 샘플 텍스트같은거 넣어" 요청대로 배경/나의 역할/과정/결과/회고/협업/기술 스택/다음 단계 8개 항목의 플레이스홀더 문단을 추가(전부 대괄호 자리표시자 — 실제 콘텐츠 아님). Playwright로 `scrollHeight`가 `clientHeight`를 실제로 초과하는지, `scrollTop`을 움직이면 실제로 내용이 스크롤되고 이미지는 그대로 있는지까지 확인 | index.html, modal.css | "모달창 아래로 스크롤되게끔 일단 샘플 텍스트같은거 넣어" 요구사항 |
| 2026-08-23 | 모달 백드롭에 `backdrop-filter: blur(10px)` 추가(Safari 대비 `-webkit-` 접두사 포함) — 배경이 이미 어두운 팔레트라 블러 자체는 opacity 페이드와 함께 은은하게만 보이지만, 뒤 갤러리 요소가 살짝 흐려지며 모달에 시각적 깊이가 더해짐. GSAP로 애니메이션하는 속성이 아니라 CSS로 고정 적용된 정적 값이라(스크롤에 연동된 반복 blur가 아님) `gsap-scroll-motion` 스킬의 성능 금지 패턴과 무관 | modal.css | "모달창열리면 백드롭필터 블럭 10px" 요구사항 |
| 2026-08-23 | 모달 닫기 애니메이션 버그 수정 — 사용자가 스크린샷에 모달 이미지 영역을 원으로 표시하며 "닫기를 눌렀을 때 이미지영역이 사라지면서 텍스트가 왼쪽으로 밀리는데 안밀리게" 제보. 실측으로 원인 특정: `.modal__stage`가 `display:flex`였는데, 닫기 애니메이션 시작 시 `.modal__media`를 `position:fixed`로 전환해(FLIP 역재생을 위해 화면 좌표에 고정) 원래의 flex 흐름에서 이탈시키자, 옆에 있던 `.modal__body`가 flex 형제 하나가 사라진 걸로 인식해 그 빈자리를 메우며 왼쪽으로 확 당겨짐(media가 사라지는 약 0.6초 내내 body가 잘못된 위치에서 페이드아웃되고 있었음) — 겉보기엔 "이미지가 사라지며 텍스트가 밀린다"로 보였지만 실제로는 옆 형제 요소가 flow에서 빠질 때 발생하는 전형적인 flex 재배치 버그. `.modal__stage`를 `display:flex`에서 `display:grid`(`grid-template-columns: 34vw 1fr`)로 바꾸고, media/body 양쪽에 `grid-column`을 명시적으로 고정(1번째 수정에서 이 부분을 빠뜨려 이번엔 반대로 media가 fixed로 빠지자 grid 자동배치가 body를 컬럼2가 아니라 컬럼1 자리로 끌어당기는 같은 종류의 버그가 재현됨 — 재검토 후 media=`grid-column:1`, body=`grid-column:2`를 각각 명시해서 완전히 해결) — grid는 트랙 크기가 명시값(`34vw`, `1fr`)이라 안에 있던 아이템이 `position:fixed`로 빠져도 트랙 자체는 크기를 유지해 형제가 흔들리지 않는다. 모바일 브레이크포인트도 동일 원리로 `grid-template-rows: 42vh auto`+명시적 `grid-row`로 맞춰 세로 스택에서도 같은 버그가 나지 않게 함. Playwright로 media가 `position:fixed`인 상태에서 body의 `getBoundingClientRect().left` 값이 열림·닫힘 내내 안정적으로 유지되는 것을 직접 실측 확인 | modal.css | "닫기를 눌렀을때 이미지영역이 사라지면서 텍스트가 왼쪽으로 밀리는데 안밀리게" 요구사항 |
| 2026-08-23 | 모달 타임라인 순서 조정 — 우측 텍스트(`.modal__body`)가 이미지 이동 애니메이션(0~0.7s)과 겹쳐서(0.3s부터) 같이 나타나던 것을, 사용자가 스크린샷에 우측 텍스트 영역을 원으로 표시하며 "이미지가 위치하고 나서 보여지게" 요청한 대로 이미지가 완전히 자리 잡은 뒤(0.7s 시점)부터 fade-in 하도록 GSAP 타임라인의 시작 오프셋만 0.3→0.7로 수정. 이 과정에서 검증 방법 자체의 함정을 하나 발견 — 단일 `page.evaluate` 호출 안에서 `setTimeout` 루프로 여러 시점을 한 번에 샘플링하면 그 호출이 진행되는 동안 실제 브라우저의 requestAnimationFrame(및 그 위에서 도는 GSAP 티커)이 진행되지 않아 애니메이션이 "멈춘 것처럼" 보이는 거짓 신호를 얻었다 — 매 시점을 별도의 도구 호출로 나눠 확인하니(왕복 지연은 있지만 그 사이 실제 프레임이 정상 진행됨) 이미지가 왼쪽에 자리 잡을 때까지 본문이 계속 opacity 0으로 대기하다가 그 이후에만 서서히 나타나는 것을 정확히 확인 | modal.js | "우측텍스트는 이미지가 위치하고나서 보여지게끔" 요구사항 |
| 2026-08-23 | GSAP 기반 상세 모달 신설 — 이미지 클릭 시 그 이미지가 화면 왼쪽 세로 가운데로 "날아가" 확대되고, 우측에 제목/태그/설명이 fade-in되는 다이얼로그. 전용 GSAP 플러그인(Flip) 없이 코어 GSAP만으로 수동 FLIP 기법 구현 — 클릭 시점 썸네일의 실제 화면 좌표(`getBoundingClientRect`)를 시작점으로 기록하고, 모달 레이아웃(왼쪽 미디어/오른쪽 텍스트)이 최종적으로 차지할 좌표를 한 번 측정한 뒤, `position:fixed`로 시작 좌표에 즉시 배치했다가 최종 좌표로 `top/left/width/height`를 튠(이미지 비율 왜곡 없이, transform scale 대신 실제 박스 크기 애니메이션 — 1회성 모달 열기 애니메이션이라 `gsap-scroll-motion` 스킬의 스크롤-스크럽 성능 원칙과는 별개로 허용). 닫기는 같은 시작 좌표로 역재생해 원래 썸네일 자리로 정확히 되돌아간 뒤 모달을 숨김(그 자리엔 이미 원본 썸네일이 그대로 있어 이음매 없이 자연스럽게 복귀). 모달이 열려있는 동안 배경 Locomotive Scroll을 `lscroll.stop()`으로 잠그고(`stopScroll`은 새 휠 입력만 막고 이미 진행 중이던 관성은 즉시 끊지 않는다는 걸 실측 확인 — 클릭 직전까지 빠르게 스크롤하던 경우에 한해 짧은 잔여 이동이 있을 수 있는 사소한 한계로 남겨둠), 닫을 때 `lscroll.start()`로 해제. 접근성: `role="dialog" aria-modal aria-hidden` 토글, Esc/배경클릭/닫기버튼 3방식 닫기, 열릴 때 닫기버튼 포커스·닫을 때 트리거로 포커스 복귀, 이미지에 `tabindex role="button"`+Enter/Space 키보드 활성화 추가. 콘텐츠는 클릭된 항목의 기존 DOM(번호/타이틀/태그)에서 그대로 읽어와 채우고(별도 데이터 소스 불필요), 설명 문단만 플레이스홀더로 고정(전 항목 공통, "일단 기능구현 완료 후 내용 교체" 방침에 따름). **실제 구현 중 발견한 진짜 버그**: `drag-scroll.js`가 모든 `pointerdown`에서 즉시 `container.setPointerCapture()`를 호출하고 있었는데, 이 캡처가 걸린 상태에서 발생하는 `pointerup`과 뒤이은 `click` 이벤트는 원래 눌렀던 요소가 아니라 캡처를 건 컨테이너(MAIN)로 리타깃됨(Playwright로 진짜 마우스 클릭의 이벤트 타깃을 직접 로깅해 확인) — 그 결과 이미지를 "클릭"만 해도(드래그 없이) 실제 클릭 이벤트가 이미지가 아니라 스크롤 컨테이너에서 발생해 모달이 절대 열리지 않는 상태였다. 포인터 캡처 시점을 pointerdown 즉시가 아니라 "누적 이동량이 임계값(6px)을 넘어 진짜 드래그로 판정된 순간"으로 늦춰 해결 — 단순 클릭은 캡처가 전혀 걸리지 않아 click 이벤트가 정상적으로 원래 타깃에 도달하고, 실제 드래그만 캡처+휠 시뮬레이션 경로를 탄다. Playwright로 데스크톱 오픈/클로즈 애니메이션·배경 스크롤 잠금·키보드 Esc·모바일 세로 스택 레이아웃·드래그 시 모달 오발동 없음까지 전부 실측 확인 | index.html, modal.css(신규), modal.js(신규), drag-scroll.js | "이제 모달창 만들꺼야 gsap.js활용해서 이미지클릭시 이미지가 화면 왼쪽 세로 가운데로 이동하고 우측에 텍스트가 fade-in형태로 보이는 느낌으로" 요구사항 |
| 2026-08-23 | 사용자가 스크린샷에 장식용 대형 아웃라인 텍스트("verjuice"/"zyrian")를 원+화살표로 표시하며 "슬라이드 맨앞으로 이동" 요청, 이어서 "'텍스트+슬라이드+슬라이드…'가 하나의 그룹"이라는 설명 추가(콘텐츠는 나중에 교체할 것이므로 지금은 구조만 반영). 기존엔 이 텍스트가 item4와 item5 "사이"(사실상 뒤 그룹인 5~8의 리드 텍스트 역할)에 있었는데, 갤러리 트랙의 진짜 맨 앞(`<div class="gallery">`의 첫 자식, item1보다도 앞)으로 옮겨 이제 갤러리에 진입하자마자 이 텍스트가 가장 먼저 보인다. 나머지 두 장식 텍스트(Tiffin/Skanky, Deasil/Zetetic)는 지목되지 않아 손대지 않음 — 같은 처리를 원하면 후속 요청으로 진행 | index.html | "이미지 표시한 텍스트영역 슬라이드 맨앞으로 이동", "텍스트+슬라이드+슬라이드… 이 구성이 하나의 그룹" 요구사항 |
| 2026-08-23 | 사용자가 스크린샷에 "explore" 원형 버튼을 직접 원으로 표시하며 4가지 요청: (1) 이미지 영역 중앙으로 이동 (2) 호버 시에만 `transition: all .3s ease`로 나타나게 (3) 텍스트 대신 draw-in 애니메이션이 있는 '+' 아이콘 SVG로 교체 (4) 반응형 미적용 문제 해결. 위치는 `.gallery__item-link`를 caption 그리드의 'gallery-link' 영역 대신 이미지와 같은 칸(`grid-column:2; grid-row:1/4`)에 두고 `justify-self/align-self:center`로 이미지 중앙에 오도록 재배치. 기본 `opacity:0; pointer-events:none`이었다가 `.gallery__item:hover`에서 나타나게(터치 기기는 `@media(hover:none)`으로 항상 노출 — hover 개념이 없는 기기에서 버튼이 영영 안 보이는 걸 방지). 12개 항목의 `<a>` 안 "explore" 텍스트를 사용자가 준 Material Line Icons '+' SVG로 전량 교체. 처음엔 SVG의 SMIL `<animate fill="freeze">`를 그대로 남겼는데, 이는 로드 시 딱 한 번만(버튼이 `opacity:0`으로 숨어있는 동안) 그려지고 끝나 이후 호버해도 이미 다 그려진 정적 아이콘만 보이는 문제가 있었다 — 사용자가 "이미지 호버시 아이콘 애니메이션이 동작하게끔" 요청. SMIL을 걷어내고 `stroke-dasharray:16; stroke-dashoffset:16→0`을 `.gallery__item:hover`에 물린 CSS `transition`으로 교체해 호버할 때마다 매번 다시 그려지도록 함(두 번째 path는 `transition-delay`로 살짝 늦게 그어져 원본 SMIL의 순차 드로잉 느낌 유지), 터치 기기 fallback에도 동일하게 dashoffset:0 고정 추가. 이어서 "호버시 배경색 안바뀌게" 요청으로 `:hover`/`:focus`의 배경·아이콘색 스왑(빨강→크림)을 제거하고 배경은 항상 빨강으로 고정(포커스만 opacity 노출 유지). 반응형은 700px 이하 미디어쿼리를 신설해 번호/타이틀 여백 컬럼을 8rem→3rem, 이미지 크기를 `50vh*0.68`로 축소(기존엔 `calc(66vh*0.75)`가 데스크톱 기준값이라 모바일 세로 화면에서 항목 폭이 화면 폭을 넘어 태그가 화면 밖으로 잘려나갔음), 태그 목록도 `grid-auto-flow:column`(줄바꿈 불가) 대신 `flex-wrap:wrap`으로 바꿔 넘치면 줄바꿈되게 함 | index.html, demo2.06b37b5f.css | "이미지영역의 중앙으로 이동, hover시 나타나게 트랜지션 all .3s ease, 안에 텍스트 내용 제거 + 아이콘으로 변경, 반응형", "이미지 호버시 아이콘 애니메이션이 동작하게끔", "호버시 배경색 안바뀌게" 요구사항 |
| 2026-08-23 | 사용자가 스크린샷에 번호("01")와 타이틀("Moonraker") 두 요소를 직접 원으로 표시하며 "열정도체로 교체" 요청 — 바로 위에서 등록만 해두고 미적용 상태였던 Rix Yeoljeongdo를 `.gallery__item-number`/`.gallery__item-title`에 실제 적용(`.gallery__text`는 지목되지 않아 기존 moret 유지). 처음엔 Regular 한 가지 두께만 있어 `font-weight:400; font-style:normal`로 붙였는데, 곧바로 "기울림 추가" 요청을 받아 `font-style:italic`으로 변경(둥글고 통통한 Rix체에 브라우저 합성 이탤릭을 적용해도 기존 moret의 기울어진 느낌과 자연스럽게 이어짐을 스크린샷으로 확인). 이어서 "폰트사이즈 동일하게" + "제목기준"(번호가 타이틀보다 커 보이던 것을 타이틀 크기 `clamp(2rem,5vw,4rem)`에 맞춰 번호 쪽을 줄임 — 원래 번호가 `clamp(2.5rem,9vw,6.5rem)`로 더 컸음) 요청 반영 | demo2.06b37b5f.css | "표시한 텍스트 열정도체로 교체", "기울림 추가", "폰트사이즈 동일하게", "제목기준" 요구사항 |
| 2026-08-24 | "모달의 Description, What I did, Skills.는 공통이니까 자리만 잡아놔" — `modal.js`의 `populate()`가 그동안 이 세 정보 그룹을 `data.description`/`whatidid`/`skills`가 비어있으면 통째로 `display:none`으로 숨기고 있어, 아직 실제 콘텐츠가 채워진 항목(item1 "Design System" 하나뿐)만 이 영역이 보이고 나머지 11개 항목은 모달을 열어도 이 세 섹션 자체가 사라져 항목마다 레이아웃이 들쭉날쭉했다. 세 그룹을 항상 표시("Description."/"What I did."/"Skills." 헤더 포함)하도록 바꾸고, 데이터가 비어있을 때만 대괄호 안내 문구(`[프로젝트 설명을 입력하세요]` 등, 이 프로젝트가 이미 쓰던 placeholder 표기 관례를 그대로 따름)로 자리를 채운다 — "사이트 보기" 링크는 프로젝트마다 있을 수도 없을 수도 있는 선택적 요소라 이번 요청 범위 밖으로 보고 기존의 있을 때만 노출하는 동작을 유지. item1(실제 데이터)과 item2(빈 데이터) 양쪽 모두 모달을 열어 회귀 없음과 placeholder 정상 노출을 Playwright로 확인, 콘솔 에러 없음도 재확인 | js/modal.js | "모달의 Description, What I did, Skills.는 공통이니까 자리만 잡아놔" 요구사항 |
| 2026-08-24 | item2("MY클라우드") 모달 콘텐츠에 실제 프로젝트(D:\MyCloud 개인용 업무 대시보드) 소개글 반영 — 사용자가 붙여넣은 Description 한 문단(AI API 연동 실험 목적, React+Tailwind/Bun+Hono+SQLite 스택, Gemini→OpenAI 폴백 캐스케이드, 멀티턴 AI 채팅, RBAC, Tailscale 원격 접속 요약)과 What I did 5개 항목·Skills 8개 항목을 `gallery__item-data`의 `description`/`whatidid`/`skills` 필드에 그대로 입력. 바로 앞서 이 세 그룹을 "항상 표시"로 바꿔둔 덕에 별도 코드 수정 없이 데이터만 채워 반영됨. 실서비스가 사내 전용(Tailscale)이라 공개 URL이 없어 `link`는 빈 값으로 유지(해당 항목은 지금처럼 "사이트 보기" 버튼 없이 노출). 타이틀("MY클라우드")·태그(#tag1~3, 아직 placeholder)·섹션2 이미지 12장은 이번 요청 범위 밖이라 손대지 않음. Playwright로 JSON 파싱 오류 없음, 모달에 설명·What I did·Skills(8개) 전부 정상 렌더링, 콘솔 에러 없음까지 확인 | index.html | (item2 모달 콘텐츠로 붙여넣은 MyCloud Description/What I did/Skills 텍스트) |
| 2026-08-24 | item2("MY클라우드")의 placeholder 태그(#tag1/#tag2/#tag3)를 실제 태그(#AI-Agent/#React·Bun/#RBAC)로 교체 — 갤러리 캡션과 모달의 `.modal__tags`가 같은 DOM(`.gallery__item-tags`)을 소스로 공유해 한 곳만 고치면 양쪽에 동일하게 반영됨을 확인. Playwright로 두 위치 모두 새 태그로 렌더링, 콘솔 에러 없음 확인 | index.html | "태그 #AI-Agent / #React·Bun / #RBAC" 요구사항 |
| 2026-08-24 | item2("MY클라우드") 모달 콘텐츠 개정 — 기존 "AI API 연동 실험" 중심 서술에서 "AI 에이전트(Claude Code)에게 자연어 프롬프트로 요구사항을 전달해 기획부터 배포까지 진행한 AI 협업 개발 프로세스"를 앞세우는 방향으로 Description·What I did(5→6개, "AI 협업 개발 프로세스 정립" 항목 신설)·Skills(1번째 항목 "AI Agent Workflow"→"AI Agent-driven Development"로 교체, 순서도 AI Agent-driven Development/Prompt Engineering/AI API Integration 순으로 재배열) 전체 교체. Playwright로 JSON 파싱·모달 렌더링(6개 항목·8개 스킬 전부 일치)·콘솔 에러 없음 확인 | index.html | "MY클라우드 내용 변경" 요구사항(Description/What I did/Skills 전체 재작성 텍스트 붙여넣기) |
| 2026-08-24 | 모달 Description에 문단 줄바꿈(멀티 파라그래프) 지원 추가 — 사용자가 직접 IDE에서 item2 `description`을 문자열 배열로 바꿔 편집을 시도했으나 "이거 왜 안돼" 문의. 원인 두 가지를 실측 확인: (1) 배열 뒤 `]`와 다음 `"link"` 필드 사이에 쉼표가 빠져 JSON 자체가 깨져있었음(`JSON.parse` 실패 → catch에서 `data={}`로 전부 placeholder로 폴백되고 있었음) — 이 과정에서 `skills` 배열도 8개→5개로 줄어 있어(사람이 손으로 편집하다 남은 흔적으로 판단) 8개로 복원. (2) 설령 JSON이 유효했더라도 `modal.js`의 `descEl.textContent = description`은 문자열 전용이라 배열을 넘기면 그대로 toString() 되어 줄바꿈 없이 이어 붙는데다, 마크업도 `<p class="modal__desc">` 단일 태그라 애초에 여러 문단을 담을 그릇이 없었음. `description`이 문자열이든 배열이든 모두 받아 문단마다 `<p>`를 생성하도록 `populate()`를 고치고(빈 배열이면 기존 placeholder 문구로 폴백), 마크업을 `<p class="modal__desc">`→`<div class="modal__desc">`(문단 컨테이너)로, CSS에 `.modal__desc p + p { margin-top:1.25rem }`로 문단 간격을 추가. 기존처럼 문자열 하나만 넣은 항목(item1)은 배열로 감싸져 문단 1개로 동일하게 렌더링돼 회귀 없음. Playwright로 item2가 4개의 개별 `<p>`로(스크린샷으로 문단 간격도 확인), item1이 기존처럼 1개 문단으로 렌더링되는 것, JSON 파싱 에러/콘솔 에러 없음을 확인 | index.html, css/modal.css, js/modal.js | "이거 왜 안돼"(description을 배열로 편집한 파일), "줄바꿈하고 싶은데" 요구사항 |
| 2026-08-25 | "이미지폴더에 아이템별 폴더 생성후 이미지 옮겼어" — 사용자가 `images/` 루트에 흩어져 있던 item1/item2 관련 파일들을 `images/item1/`, `images/item2/`(+향후 콘텐츠용 빈 `images/item3~5/`)로 직접 재배치. index.html의 참조 3곳(item1 썸네일, item1 section3 2장, item2 썸네일)과 item2 section2의 12장 배열을 전부 새 하위 폴더 경로로 갱신(`modal.js`는 `'images/' + name` 규칙을 그대로 쓰므로 JSON의 `images` 배열 값에 `item2/` 접두사를 붙이는 것만으로 처리됨). item3(제목 placeholder, 아직 미착수)는 여전히 `sample 3.png`를 참조 중이라 이번 범위에서 제외 — 새로 만들어진 `images/item3/`(item3.png+상세 9장)는 콘텐츠 작성 요청 시 반영 예정. Playwright로 새 경로 전부 fetch 200, item1 section3 2장·item2 section2 12장 모두 `complete && naturalWidth>0`, 콘솔 에러 없음 확인 | index.html | "이미지폴더에 아이템별 폴더 생성후 이미지 옮겼어" 요구사항 |
| 2026-08-25 | item3("충남이면충분해") 모달 콘텐츠 신설 — 사용자가 준 사실(에이전시 외주 의뢰로 시안 디자인 작업, 혼자 진행, 포토샵+일러스트 사용)에 "인터넷에 검색 후 관련 내용 넣을꺼 있으면 넣어" 요청이 더해져 WebSearch로 "충남이면 충분해"를 조사 — 충청남도가 내세운 "2025-2026 충남 방문의 해" 관광 슬로건/캠페인(볼거리·즐길거리·먹거리로 충남을 홍보)임을 확인하고 Description에 이 배경을 반영. 썸네일을 placeholder(`sample 3.png`)에서 실제 이미지(`images/item3/item3.png`)로 교체, section2에 상세 이미지 9장(`item3-1~9.png`)을 images 타입으로 연결(item2와 동일 패턴). 실제 열어본 썸네일이 "2025-2026 충남방문의해" 문구가 박힌 시안 그래픽이라 검색으로 파악한 캠페인명·기간과 정확히 일치함을 재확인. Skills/whatidid는 사용자가 준 사실 범위 내에서만 작성(Photoshop, Illustrator, 시안 디자인, 브랜딩·캠페인 디자인 / 에이전시 외주 단독 진행·컨셉 기획 등), 없는 정보(계약 규모·구체 산출물 형태 등)는 추측해 넣지 않음. 태그는 사용자가 이미 넣어둔 "#시안" 외 나머지 2개(#tag2/#tag3)는 지시 없어 손대지 않음, `link`도 비공개 외주 시안이라 빈 값 유지. Playwright로 JSON 파싱·9장 전부 로딩·설명 4문단/whatidid 3개/skills 4개 렌더링·콘솔 에러 없음 확인 | index.html | "'충남이면 충분해' 에이전시 업체에서 외주 의뢰 후 시안 디자인 작업함 혼자 다함... 포토샵+일러스트로 만듬... 인터넷에 충남이면 충분해 검색 후 관련 내용 넣을꺼 있으면 넣어" 요구사항 |
| 2026-08-25 | item3("충남이면충분해")의 placeholder 태그(#tag2/#tag3)를 실제 태그(#브랜딩/#포토샵·일러스트)로 채움 — 기존 #시안은 유지. 갤러리 캡션·모달 양쪽에 반영되는 것 Playwright로 확인 | index.html | "태그도 넣어봐" 요구사항 |
| 2026-08-25 | "반응형 다시 검토해" 요청으로 320px~1440px 전 구간(모바일 세로/가로, 태블릿, 데스크톱)을 Playwright로 재점검 — 기존 모달 훅스택(≤900px)·컴포넌트 그리드(≤900px/≤560px)·갤러리 아이템(≤700px) 반응형은 전부 정상 확인. 실제 버그 2건 발견·수정: (1) `html`이 `-4deg` 회전된 `.rotate` 래퍼 때문에 실제 콘텐츠보다 약 12~28px 더 큰 바운딩 박스를 갖는데, 이 여분이 데스크톱(≥1024px)에서는 Locomotive의 `has-scroll-smooth` 클래스가 거는 `overflow:hidden`에 가려 안 보이다가, Locomotive가 자체적으로 `smooth`를 꺼버리는(자체 `tablet.breakpoint:1024` 로직 — 너비가 1024px 미만이면 터치 여부와 무관하게 "모바일"로 판정) 태블릿/모바일 폭에서는 그 잠금이 빠져 실제로 살짝 세로 스크롤 가능한 여백으로 노출되고 있었다(실 사용상 눈에 띄는 콘텐츠 변화는 없지만 터치 기기에서 러버밴드 바운스로 체감될 수 있는 결함) — `html{overflow-y:hidden}`을 무조건 규칙으로 추가해 스무스 모드 여부와 무관하게 세로 오버플로를 원천 차단(가로는 이 폭에서 네이티브 document 스크롤로 실제 내비게이션에 쓰이므로 손대지 않음), 실제 트러스티드 휠 입력(page.mouse.wheel)으로 스크롤이 0에 고정되는 것 확인. (2) 이전 세션에 구현한 데스크톱 마우스 드래그 스크롤(`drag-scroll.js`)이 Locomotive의 Smooth 모듈 전용 내부 API(`lscroll.scroll.updateDelta`)를 무조건 호출하고 있어, 방금 확인한 것과 같은 이유로 Locomotive가 Native 모듈로 전환되는 1024px 미만 폭(또는 실제 터치 기기가 넓은 화면이어도 UA로 모바일 판정되는 경우, 예: 아이패드 가로모드)에서는 이 메서드 자체가 없어 매 pointermove마다 `TypeError`가 발생하며 드래그 스크롤이 완전히 죽어있었다(Playwright 트러스티드 마우스 드래그로 재현 — 콘솔에 초당 여러 건씩 에러 누적, scrollX 불변) — `typeof updateDelta === 'function'`으로 분기해 Smooth 모듈이 없을 때는 `window.scrollBy(-dx*배율, 0)`로 네이티브 가로 스크롤에 직접 반영하도록 폴백 추가, 방향(왼쪽 드래그=전진)도 기존 관례와 동일하게 유지. 두 버그 모두 각 원인 폭 구간(390/320/820/844×390 랜드스케이프)에서 실측 재현 후 수정 확인, 데스크톱(1440px) 경로는 회귀 없음 재확인, 전 구간 콘솔 에러 0건 | css/base.css, js/drag-scroll.js | "반응형 다시 검토해" 요구사항 |
| 2026-08-23 | "화면 꽉차게" + "헤더부분 삭제" 요청 반영 — (1) 데모 브랜딩용 상단 `.frame`(코드롭스 타이틀/Demo01~05 전환 링크/Previous demo·Article·GitHub 링크)을 마크업에서 완전히 제거하고, base CSS의 관련 규칙(`.frame*`, 프레임 유무에 따라 갈라지던 `.content` 높이 분기)도 함께 정리해 `.content`가 항상 `height:100vh; justify-content:center`이도록 단순화. demo2 스타일시트에 남아있던 프레임 전용 미디어쿼리 잔재도 제거. (2) 헤더를 없애도 갤러리 이미지 자체 크기(`21vmax×28vmax`)가 작아 화면 상하에 빈 여백이 많이 남던 문제를 확인, 이미지 행 높이를 `vmax` 대신 `vh` 기반(`calc(66vh*0.75)` 폭 × `66vh` 높이)으로 바꿔 데스크톱(가로 폭이 큰 화면)뿐 아니라 세로로 긴 모바일 화면에서도 뷰포트 높이 대비 일관되게 꽉 차 보이도록 함(기존 `vmax`는 가로/세로 중 큰 값 기준이라 모바일 세로 화면에서 이미지가 뷰포트 높이의 40%밖에 안 채웠던 것을 80%대로 개선) | index.html, base.98fd6c19.css, demo2.06b37b5f.css | "화면 꽉차게", "헤더부분 삭제" 요구사항 |
